// minio — CI/CD (HAP-404, онбординг в новый Jenkins).
//
// Конфигурация сборки живёт ЗДЕСЬ, в репозитории проекта, а не в настройках Jenkins —
// требование HAP-388. Общие шаги (composeDeploy/notify) — в библиотеке happydebt:
// https://github.com/ServicePCT/jenkins-shared-library
//
// У minio нет ни своей сборки, ни тестов: это готовый образ с конфигурацией. Поэтому
// pipeline короткий — только выкатка и проверка, что стек поднялся.

@Library('happydebt') _

pipeline {
    agent any

    environment {
        // Ключи выкатки — из хранилища ПАПКИ `crm`, а не из общего (HAP-500). Общий
        // deploy-staging-ssh виден сборке любого подключённого проекта: сборка исполняет
        // Jenkinsfile из своего репозитория, и запросить чужой credential по id ей ничто
        // не мешает.
        // Папка общая с crm, и это не про предметную область: CRM ходит в это хранилище ROOT-кредами
        // MinIO, то есть держит их у себя в .env. Отдельная папка нарисовала бы границу, которой
        // нет; своя появится, когда у CRM будет сервисный аккаунт вместо root.
        DEPLOY_CREDENTIALS_ID = 'deploy-crm-staging-ssh'
        // Прод-ключа ещё нет — как и прод-хоста. Идентификатор проставлен заранее, чтобы
        // первая прод-выкатка упала с «credential not found», а не уехала на боевую машину
        // стендовым ключом.
        PROD_CREDENTIALS_ID   = 'deploy-crm-prod-ssh'
    }

    parameters {
        // Целевые хосты — параметрами с дефолтами прямо здесь.
        // ⚠️ При смене дефолта помните: Jenkins применяет новое значение со ВТОРОЙ сборки
        // (см. jenkins-infra/DEPLOY.md §10). После правки прогоните сборку дважды.
        string(name: 'STAGING_HOST', defaultValue: 'prog@vm-stage.happydebt.kz', description: 'user@host стенда; пусто — выкатка пропускается')
        string(name: 'STAGING_PATH', defaultValue: '/srv/minio', description: 'каталог с docker-compose.yml на стенде')
        string(name: 'PROD_HOST',    defaultValue: '', description: 'user@host прода; пусто — выкатка пропускается')
        string(name: 'PROD_PATH',    defaultValue: '/srv/minio', description: 'каталог с docker-compose.yml на проде')
        // Выкатка по ВЕРСИИ (HAP-825). Клиентские инсталляции обновляются на тег релиза, а
        // не на main (правило 2 тиражирования, HAP-799). Отдельной джобы для тега нет: тег —
        // не ветка, и плодить по джобе на каждый релиз незачем. Версия приезжает параметром,
        // джоба берётся штатная, с основной ветки.
        //
        // Пусто — прежнее поведение целиком: собирается и катится коммит ветки.
        string(name: 'DEPLOY_REF', defaultValue: '',
               description: 'тег релиза (или коммит) для выкатки на клиентскую инсталляцию. Пусто — собранный коммит ветки. Контракт: jenkins-infra/docs/release-and-update.md')
    }

    options {
        timeout(time: 20, unit: 'MINUTES')
        buildDiscarder(logRotator(numToKeepStr: '30'))
        timestamps()
        // disableConcurrentBuilds() убран (HAP-825). Он защищал по ДЖОБЕ, а гонка возможна
        // только по ЦЕЛИ — двум выкаткам в один каталог на одном хосте. Пока стенд был один,
        // разницы не было; с 10–15 клиентскими инсталляциями «клиент» — это значение
        // STAGING_HOST, и замок по джобе означает, что клиенты обновляются строго по очереди.
        // Замок по цели ("deploy:<host>:<path>") ставит сам composeDeploy: разные машины
        // обновляются параллельно, один каталог — по-прежнему по одному.
    }

    stages {
        stage('проверка конфигурации') {
            // Обновление клиента на УЖЕ ВЫПУЩЕННУЮ версию не пересобирает и не перепроверяет
            // её: тег проходил все гейты, когда его резали. Прогон здесь был бы не «лишней
            // проверкой», а проверкой ДРУГОГО кода — рабочая копия сборки стоит на основной
            // ветке, а выкатывается тег (HAP-825).
            when { expression { !params.DEPLOY_REF?.trim() } }
            steps {
                // Своих тестов у проекта нет, но синтаксис compose проверить стоит: сломанный
                // YAML лучше поймать здесь, чем на целевом хосте в середине выкатки.
                //
                // Обязательные переменные подставляем заглушками прямо здесь. Через
                // --env-file .env.example не выйдет: там пароли намеренно пустые, а
                // ${VAR:?} на пустом значении уронит проверку. Реальные секреты в CI не
                // нужны — `config` только раскрывает подстановки и проверяет структуру.
                sh '''
                    set -eu
                    MINIO_ROOT_USER=ci-check \
                    MINIO_ROOT_PASSWORD=ci-check-placeholder \
                    docker compose -f docker-compose.yml config >/dev/null
                    echo "docker-compose.yml валиден"

                    # Скрипт инициализации исполняется на целевом хосте внутри контейнера
                    # mc, где отладчика нет и логи видны только постфактум. Синтаксис
                    # ловим здесь: ошибка в нём означает стек без бакетов и без учётки CRM.
                    sh -n _docker/init.sh
                    echo "_docker/init.sh валиден"
                '''
            }
        }

        stage('deploy staging') {
            when {
                allOf {
                    // Имя основной ветки у наших репозиториев разное (у minio — dev),
                    // поэтому опираемся на флаг branch-api, а не на литерал.
                    expression { env.BRANCH_IS_PRIMARY == 'true' }
                    expression { params.STAGING_HOST?.trim() }
                }
            }
            steps {
                composeDeploy(
                    env: 'staging',
                    host: params.STAGING_HOST,
                    path: params.STAGING_PATH,
                    credentialsId: env.DEPLOY_CREDENTIALS_ID,
                    // Выкатка по версии (HAP-825): null => composeDeploy возьмёт env.GIT_COMMIT,
                    // то есть прежнее поведение. Непусто — на хосте будет checkout тега, и в лог
                    // уедет строка «выкачено: ref=… sha=… describe=…».
                    ref: params.DEPLOY_REF?.trim() ?: null,
                    // 🔴 envSecrets здесь БОЛЬШЕ НЕТ (HAP-806). Ключи всех трёх сервисных
                    // аккаунтов (CRM — HAP-509, телефония — HAP-516, file_service —
                    // HAP-620) живут только в .env на хосте.
                    //
                    // Почему убрали. Credential в Jenkins — ОДИН НА ПАПКУ, а «на какой
                    // хост катим» здесь не измеряется: джоба перенацеливается параметром
                    // STAGING_HOST, а набор credential'ов остаётся тем же. Значит одни и
                    // те же ключи уезжали на ВСЕ инсталляции разом. Измерено на живых
                    // машинах: все шесть ключей отсюда совпали на vm-stage и vm-test. Для
                    // клиентских инсталляций это означало бы, что один слитый ключ
                    // открывает записи разговоров и файлы должников во всём парке.
                    //
                    // Что теперь источник: ключ рождается на самой машине и пишется сразу
                    // во все .env, где нужен, — и тому, кто учётку ЗАВОДИТ (этот сервис),
                    // и тому, кто ею ХОДИТ (crm, file_service, telephony-ari). Это
                    // jenkins-infra: pipelines/new-host/host-secrets.sh, фаза 0
                    // new-host-deploy. Разбор — jenkins-infra/docs/secrets-model.md.
                    //
                    // Заодно исчезает перекрёстная зависимость, которую приходилось
                    // держать в голове: ключи телефонии лежали в папке `crm`, потому что
                    // учётку заводит сборка minio, а до потребителя те же значения ехали
                    // отдельным Secret-file credential'ом папки `telephony` — два
                    // независимых источника одного значения, которые обязаны совпадать.
                    // Источник стал один, совпадать нечему.
                    //
                    // На работающих машинах этой правкой не меняется НИЧЕГО: значения уже
                    // лежат в .env, выкатка просто перестаёт переписывать их поверх.
                    //
                    // Root-креды хранилища сюда и раньше не ехали — они на хосте с самого
                    // начала. Теперь по тому же принципу живут и остальные.
                    approve: false
                )
            }
        }

        stage('deploy prod') {
            when {
                allOf {
                    expression { env.BRANCH_IS_PRIMARY == 'true' }
                    expression { params.PROD_HOST?.trim() }
                }
            }
            steps {
                // 🔴 Прод — только с явного подтверждения. В хранилище лежат записи разговоров
                // и файлы WhatsApp; перезапуск означает недоступность файлов для всех
                // сервисов, которые в него ходят.
                composeDeploy(
                    env: 'prod',
                    host: params.PROD_HOST,
                    path: params.PROD_PATH,
                    credentialsId: env.PROD_CREDENTIALS_ID,
                    // Выкатка по версии (HAP-825): null => composeDeploy возьмёт env.GIT_COMMIT,
                    // то есть прежнее поведение. Непусто — на хосте будет checkout тега, и в лог
                    // уедет строка «выкачено: ref=… sha=… describe=…».
                    ref: params.DEPLOY_REF?.trim() ?: null,
                    // envSecrets убраны и здесь (HAP-806) — см. пояснение у выкатки на
                    // стенд. Отдельные прод-credential'ы больше не нужны: ключи каждой
                    // машины рождаются на ней самой, поэтому «стендовый ключ на боевом
                    // хранилище» стал невозможен не по дисциплине, а по построению.
                    approve: true
                )
            }
        }
    }

    post {
        always  { notify(currentBuild.currentResult) }
        cleanup { cleanWs() }
    }
}
